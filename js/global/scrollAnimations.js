const settings = {
  duration: 1000,
  distance: '100px',
  origin: 'bottom',
  scale: 1,
  delay: 100,
  useDelay: 'once'
}

const scrollAnimations = window.sr = ScrollReveal().reveal('.fadeUp', settings, 60)

// beep
